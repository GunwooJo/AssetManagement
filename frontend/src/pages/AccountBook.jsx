import React from "react";
import { View, Text } from 'react-native';
import { Button } from "react-native-paper";

export default function AccountBook({ navigation }) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
          <Text>가계부입니당~</Text>
        </View>
      );
}